<!-- views/cars-list.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Masini de Vanzare</title>
    <link rel="stylesheet" href="/css/styles.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
      /* Specific styles for this page */
      .coming-soon {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 80vh; /* Match the height of main content from styles.css */
        background-color: #000; /* Black background */
      }
      
      .coming-soon h1 {
        color: #fff; /* White text */
        font-size: 4em; /* Big text */
        text-align: center;
      }
      .info-bar{
        background-color: #111; /* Dark background to match your theme */
        color: #fff; /* Light text for contrast */
        padding: 10px;
        display: flex;
        justify-content: space-evenly; /* Space out the two items */
        align-items: center;
      }
      .info-item {
        margin: 0 10px;
        font-size: 1rem; /* Adjust the size as needed */
      }
      .info-item i {
        margin-right: 5px; /* Space between the icon and text */
      }
    </style>
</head>
<body>
    
  <div class="info-bar">
    <div class="info-item">
      <i class="fas fa-phone"></i>info: +40 123 456 789
    </div>
    <div class="info-item">
      <i class="fas fa-money-bill-wave"></i>Finantare: +40 770 187 601
    </div>
  </div>

  <!-- Navigation Bar -->
  <header>
    <nav>
      <ul>
        <li><a href="/">Acasă</a></li>
        <li><a href="/masini-de-vanzare">Mașini de vânzare</a></li>
        <li><a href="/despre-noi">Despre noi</a></li>
        <li><a href="/finantare">Finanțare</a></li>
        <li><a href="/contact">Contact</a></li>
        <li><a href="/profesional">Profesional</a></li>
      </ul>
    </nav>
  </header>
    <h1>Mașini de Vânzare</h1>

    <div class="cars-grid" id="carsGrid">
        <% cars.forEach(car => { %>
            <div class="car-item">
                <a href="/car/<%= car.id %>" class="car-link">
                    <div class="image-wrapper">
                        <img src="<%= car.image_path %>" alt="<%= car.marca %> <%= car.model %>" class="car-image">
                    </div>
                    <div class="car-details">
                        <h3><%= car.marca %> <%= car.model %></h3>
                        <p><%= car.capacitate_cilindrica %> cm³, <%= car.combustibil %>, <%= car.putere %> CP, <%= car.an_fabricatie %></p>
                        <p class="price-tag">Preț: €<%= car.price %></p>
                        <a href="/car/<%= car.id %>" class="view-details">Vezi Detalii</a>
                    </div>
                </a>
            </div>
        <% }); %>
    </div>

    <a href="/" class="back-to-home">Înapoi la Acasă</a>

    <style>
        .cars-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(380px, 1fr));
            gap: 25px;
            margin: 20px;
        }

        .car-item {
            background: rgba(50, 50, 50, 0.7); /* Dark grey with 70% transparency */
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.5);
            text-align: center;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .car-item:hover {
            transform: scale(1.05);
            box-shadow: 0 0 25px rgba(0, 0, 0, 0.8);
        }

        .car-link {
            text-decoration: none;
            color: inherit; /* Ensure link color doesn't change */
            display: block;
        }

        .image-wrapper {
            width: 100%;
            height: 400px; /* Set height to maintain consistency */
            overflow: hidden; /* Hide any overflow to maintain size consistency */
            margin-bottom: 15px; /* Space between image and car details */
            border-radius: 5px; /* Keep border-radius for a smoother look */
        }

        .car-image {
            width: 100%;
            height: 100%;
            object-fit: cover; /* Ensure the image fills the container while maintaining aspect ratio */
            transition: transform 0.3s ease; /* Add slight zoom effect on hover */
        }

        .car-item:hover .car-image {
            transform: scale(1.05); /* Zoom in slightly when hovered */
        }

        .car-details {
            margin-top: 10px;
        }

        .car-details p {
            font-size: 16px;
            color: #fff;
        }

        .price-tag {
            color: #ff4500; /* Orange Red color for the price tag */
            font-weight: bold;
            font-size: 18px; /* Slightly larger font for the price */
        }

        .view-details {
            display: inline-block;
            margin-top: 15px;
            padding: 12px 20px;
            background-color: #b22222; /* Firebrick Red */
            color: #fff;
            text-decoration: none;
            border-radius: 4px;
            transition: background-color 0.3s ease;
        }

        .view-details:hover {
            background-color: #ff4500; /* Orange Red */
        }

        @media (max-width: 768px) {
            .cars-grid {
                grid-template-columns: 1fr;
            }
        }

        .back-to-home {
            display: block;
            margin: 20px 0;
            color: #b22222;
            text-decoration: none;
            font-size: 16px;
        }

        .back-to-home:hover {
            color: #ff4500; /* Orange Red */
        }
    </style>
    <footer>
        <div class="footer-content">
          <p>© 2025 Autoprc RolCris. Toate drepturile rezervate.</p>
          <nav>
            <ul>
              <li><a href="/">Acasă</a></li>
              <li><a href="/masini-de-vanzare">Mașini de vânzare</a></li>
              <li><a href="/despre-noi">Despre noi</a></li>
              <li><a href="/finantare">Finanțare</a></li>
              <li><a href="/contact">Contact</a></li>
              <li><a href="/profesional">Profesional</a></li>
            </ul>
          </nav>
          <div class="finance-form">
            <h3>Finanțare</h3>
            <form action="/finantare" method="POST">
              <input type="text" name="name" placeholder="Nume" required>
              <input type="email" name="email" placeholder="Email" required>
              <input type="tel" name="phone" placeholder="Telefon" required>
              <button type="submit">Trimite</button>
            </form>
          </div>
        </div>
      </footer>
    
      <!-- Floating Buttons -->
      <a href="https://wa.me/your-number" class="floating-btn whatsapp" target="_blank">WhatsApp</a>
      <a href="/finantare" class="floating-btn finantare">Finanțare</a>
    
      <!-- Include JS files -->
      <script src="/js/main.js"></script>
</body>
</html>
